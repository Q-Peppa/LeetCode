/**
 *
 * @param key keyof Math
 * @param arg number
 */
type p = Parameters<typeof Math.abs> extends number ? true : false;

const useMath = (key: keyof Math, arg: Parameters<Math[tykey]>) => {
    return 20
}

console.log([...new Array(10)]);
console.log([...new Array(10)]);
